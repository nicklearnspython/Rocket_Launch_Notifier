# Copy to .env and fill in real values. Never commit .env.
# Variable names must match the *_env entries in config.toml.
PUSHOVER_TOKEN=your-pushover-application-token
PUSHOVER_USER_ME=your-pushover-user-key
